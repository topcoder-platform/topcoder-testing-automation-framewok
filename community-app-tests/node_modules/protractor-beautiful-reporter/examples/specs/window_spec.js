describe('example', () => {

    it('should switch to pop up window', async () => {
        const handles = await browser.getAllWindowHandles();
        await browser.switchTo().window(handles[1]); // switch to Window B
        //expect(await browser.getTitle()).toBe('yyy');
    });

    it('should click accept button', async () => {
       // await acceptBtn.click(); // Window B will be clocsed once click accept button
        const handles = await browser.getAllWindowHandles();
        expect(1).toBe(handles.length);
    });

    it('should switch main window', async () => {
        const handles = await browser.getAllWindowHandles();
        await browser.switchTo().window(handles[0]); // switch back to Window A
        // I believe the exception was thrown here.
       // expect(await browser.getTitle()).toBe('xxxx');
    });

});